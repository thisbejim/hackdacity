import React from 'react';

// react-router
import { browserHistory } from 'react-router';

// material-ui
import {
  FlatButton
} from 'material-ui';

export const Dashboard = (props) => {
    return (
      <span>
        dashboard
      </span>
    )
}
